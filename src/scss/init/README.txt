This folder contains the scss mixins, variables, and functions that are referenced elsewhere in the theme.
To use these definitions in an scss file add "import '@init'" to the top of the scss file.

The files in this folder are also included in the Style Guide output.